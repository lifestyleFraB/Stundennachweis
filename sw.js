self.addEventListener('fetch', (event) => {
  // Dieses Skript fängt Netzwerkanfragen ab, macht hier aber noch nichts Komplexes.
  // Es ist notwendig, damit der Browser die App als "installierbar" erkennt.
});
